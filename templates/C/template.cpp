#include<iostream>
#include<cstdio>
#include<cstring>
#include<string>
#include<map>
#include<vector>
#include<queue>
#include<algorithm>

#define foreach(i,B,N) for(int i = B; i < N; i++)
#define debug true
#define trace(x) 	if(debug)printf(x);
#define INF 0x7FFFFFFF

using namespace std;


int main() {
	
	return 0;
}
